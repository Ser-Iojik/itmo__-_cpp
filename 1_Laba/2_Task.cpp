#include <iostream>
using namespace std;

int main() {
    int a, b;
    freopen("aplusbb.in", "r", stdin);
    freopen("aplusbb.out", "w", stdout);
    cin >> a >> b;
    cout << a + b*b << endl;
    return 0;
}