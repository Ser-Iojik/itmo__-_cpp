#include <iostream>
using namespace std;

int Quicksort(int arr[], int n) {
    
}

int main() {
    freopen("sort.in", "r", stdin);
    freopen("sort.out", "w", stdout);
    long n, a_min;
    int i, j, pivot, L, G;
    cin >> n;
    long a[n];

    for (i = 0; i < n; i++) {
        cin >> a[i];
    }

    int QuickSort(a[n]) {
        if (length(a[n]) < 2) {
            return a[n];
        } else {
            pivot = a[0];
            return 0;
        }
        return 0;
    }

    return 0;
}